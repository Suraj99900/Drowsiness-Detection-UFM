# Drowsiness-Detection-UFM
Created a drowsiness detection System (UFM) using Falsk  module

First install

run :- pip install virtualenv

run :- virtualenv env (if not work then run below command)

run :- python -m venv env


run the requirement.txt file and insured you have cmake library

run :- pip install -r requirement.txt

